// Spinner.jsx
import React from 'react';
import styles from './Spinner.module.css'; // We will create a simple CSS spinner

const Spinner = () => {
  return (
    <div className={styles.spinner}></div>
  );
};

export default Spinner;
